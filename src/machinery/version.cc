/* This is free and unencumbered software released into the public domain. */

#ifdef HAVE_CONFIG_H
#include <config.h>
#endif

#include "version.h"

const char* const machinery_version_string = MACHINERY_VERSION_STRING;

const short machinery_version_major = MACHINERY_VERSION_MAJOR;

const short machinery_version_minor = MACHINERY_VERSION_MINOR;

const short machinery_version_patch = MACHINERY_VERSION_PATCH;
